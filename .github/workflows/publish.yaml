name: publish

on:
  push:
    paths:
      - 'package.json'
      - 'pnpm-lock.yaml'
      - 'apps/electron-app-bundler/src/**'
      - 'apps/electron-app-bundler/tests/**'
      - 'apps/electron-app-bundler/package.json'
      - 'apps/electron-app-bundler/tsconfig.json'
      - 'apps/electron-app-bundler/tsconfig.test.json'
      - 'apps/electron-app-bundler/jest.config.js'
      - '.github/workflows/publish.yaml'

permissions: write-all

jobs:
  test:
    name: 🧪 Test
    runs-on: ubuntu-latest

    steps:
      - name: 👍 Checkout
        uses: actions/checkout@v6

      - name: ⚡ Setup pnpm
        uses: pnpm/action-setup@v5

      - name: ❇️ Setup Node
        uses: actions/setup-node@v6
        with:
          node-version: lts/jod
          cache: pnpm
          cache-dependency-path: ./pnpm-lock.yaml

      - name: ⬇️ Install Dependencies
        run: pnpm install --frozen-lockfile

      - name: 🔍 Audit dependencies
        run: pnpm bundler:audit

      - name: 🛠️ Build
        run: pnpm bundler:build

      - name: 🧑‍🔬 Execute Tests
        run: pnpm bundler:test

      - name: 📊 Publish Test Report
        uses: ctrf-io/github-test-reporter@v1
        with:
          report-path: './apps/electron-app-bundler/ctrf/*.json'
        if: always()

  release:
    name: 🚀 Publish
    runs-on: ubuntu-latest
    needs: test
    if: github.ref == 'refs/heads/main'

    steps:
      - name: 👍 Checkout
        uses: actions/checkout@v6
        with:
          fetch-depth: 0

      - name: 🚀 Release in repository
        uses: justincy/github-action-npm-release@2.0.2
        id: release
        with:
          path: ${{ github.workspace }}/apps/electron-app-bundler

      - name: ⚡ Setup pnpm
        if: steps.release.outputs.released == 'true'
        uses: pnpm/action-setup@v5

      - name: ❇️ Setup Node
        if: steps.release.outputs.released == 'true'
        uses: actions/setup-node@v6
        with:
          node-version: lts/jod
          cache: pnpm
          cache-dependency-path: ./pnpm-lock.yaml

      - name: 📥 Install Dependencies
        if: steps.release.outputs.released == 'true'
        run: pnpm install --frozen-lockfile

      - name: 🛠️ Build
        if: steps.release.outputs.released == 'true'
        run: pnpm bundler:build

      - name: 🚀 Publish to npm
        if: steps.release.outputs.released == 'true'
        run: pnpm publish --access public --provenance --no-git-checks
        working-directory: ./apps/electron-app-bundler
        env:
          NODE_AUTH_TOKEN: ${{ secrets.NPM_PUBLISH_TOKEN }}
